# FILES
defaults:  # (these specify which config FILES to use)
  ############## TRAIN ###################################
  - train/config: splade_toy
  - train/data: toy
  - train/model: splade
  ############## INDEX ###################################
  - index: toy
  ############## RETRIEVE ################################
  - retrieve_evaluate: toy
  ############### FLOPS ##################################
  - flops: toy

# Direct PARAMETER setting
config:  # to be provided for each run
  checkpoint_dir: ???
  index_dir: ???
  out_dir: ???
  fp16: false
  index_retrieve_batch_size: 1024
  max_length: 32
data:
  COLLECTION_PATH: data/msmarco/train_queries/queries
  # COLLECTION_PATH: data/msmarco/TREC_DL_2019/queries_2019